'''
Nella camera dei catalizzatori mescoli due reagenti base creando il primo composto: realizza `add(x, y)` in modo che ritorni la somma dei due argomenti. Mantieni la firma e lascia che gli esperimenti (i test) riescano.
'''

def add(x: int | float, y: int | float) -> int | float:
    
    return x + y