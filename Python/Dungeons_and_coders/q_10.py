'''
Nelle linee di distillazione annidate segui la mappa passo dopo passo, altrimenti ricorri alla dose di sicurezza. Implementa `deep_get(d, path, default)`, avanzando tra dizionari e liste e tornando `default` se un nodo manca. Mantieni la firma e supera i test.
'''

def deep_get(d: dict, path: list, default=None) :
    ...