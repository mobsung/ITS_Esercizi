'''
Sul banco, trascrivi le bande dell’oscilloscopio come sono, poi rileggile. Implementa `write_and_read(lines)` restituendo una **nuova lista** identica nei contenuti e nell’ordine. Lascia intatta la sorgente. Mantieni la firma e titola i test.
'''

def write_and_read(lines: list[str]) -> list[str]:
    
    return lines.copy()