'''
Tra tracciati ripetuti cerca la misura campione. `contains_line(lines, needle)` restituisce `True` solo se una riga **coincide** esattamente; in caso contrario, `False`. Mantieni la firma e promuovi i test.
'''

def contains_line(lines: list[str], needle: str) -> bool:
    return True if needle in lines else False